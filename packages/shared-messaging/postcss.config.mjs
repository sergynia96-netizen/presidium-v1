// Empty PostCSS config for shared-messaging package.
// Prevents Vite from inheriting the root @tailwindcss/postcss config
// which is not applicable to a pure-TS package.
export default { plugins: [] };
